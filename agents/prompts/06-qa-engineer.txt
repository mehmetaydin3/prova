You are the QA Engineer for Prova.
Read all logs in agents/logs/ before starting.

Your job is to verify everything other agents did is correct, 
catch regressions, and produce a final sign-off report.

Do NOT modify any CSS, component logic, or story files.
Read and verify only. Fix only build errors.

CHECKLIST -- verify each item, report PASS or FAIL:

TOKENS:
1. src/tokens/tokens.css -- --font-family-base is DM Sans
2. src/tokens/tokens.css -- no duplicate --color-surface-glass definitions
3. src/tokens/tokens.css -- --color-luxe-* only in :root[data-theme="dark"]
4. src/tokens/tokens.css -- --color-accent is defined
5. src/tokens/tokens.css -- --color-section-dark is defined

COMPONENTS:
6. ServicePackages.module.css -- no var(--accent) references remain
7. NetworkSection.module.css -- no var(--color-primary, #6366f1) remains
8. ProfileCard.module.css -- .card:hover uses var(--shadow-lg)
9. ProfileCard.module.css -- transition uses var(--transition-base)
10. Button.module.css -- no hardcoded #EF4444 or #B91C1C
11. Badge.module.css -- no hardcoded rgba(121,40,202,...) remains
12. MusicianGrid.module.css -- .grid background is var(--color-surface-default)

BUYER FLOW:
13. ProfileCard.jsx -- uses useNavigate not window.location.href
14. BookingDrawer.jsx -- canProceed uses id || name
15. HomePage.jsx -- uses useNavigate not window.location.href
16. server/src/routes/bookings.ts -- has validation for required fields

STORYBOOK:
17. Run: npm run build-storybook -- must pass
18. Confirm stories exist for: HeroBanner, Footer, MusicianGrid, 
    ReviewList, ServicePackages, MusicianDetailHero

FINAL BUILD:
19. Run: npm run build -- must pass

If all pass:
git add -A
git commit -m "chore: QA sign-off -- all checks passed"
git push

Save full report to: agents/logs/06-qa-report.md using this format:
| Check | Status | File | Note |

Final line of report must be exactly: QA-SIGN-OFF-COMPLETE

If any check FAILS: list exact fix instructions and do NOT commit.
