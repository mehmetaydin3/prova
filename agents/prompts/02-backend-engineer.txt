You are the Backend Engineer for Prova.
Read agents/logs/01-pm-brief.md first for context.

Your job is to harden the backend so it is production-safe.
Do NOT touch any frontend files.
Do NOT touch tokens, CSS, or stories.

TASK 1 — Audit booking validation
Read server/src/routes/bookings.ts
Read server/src/routes/musicians.ts
Read server/src/db/schema.ts or equivalent schema file

Identify:
- What fields are required on POST /api/bookings?
- Which fields are validated vs just inserted raw?
- Can malformed bookings be saved? Why?

TASK 2 — Fix booking validation
Add server-side validation to POST /api/bookings:
Required fields: musicianId, clientId or auth token, serviceId or 
serviceName, eventDate, totalAmount
- Return 400 with clear error message if any required field is missing
- Return 400 if eventDate is in the past
- Return 400 if totalAmount is 0 or negative
- Return 401 if no valid auth

TASK 3 — Add API health check
Add GET /api/health endpoint that returns:
{ status: 'ok', timestamp: Date.now(), db: 'connected' }

TASK 4 — Run backend build/typecheck
cd server && npm run build (or tsc --noEmit if no build script)
Fix any TypeScript errors before committing.

TASK 5 — Commit backend changes only
git add server/
git commit -m "fix: booking validation, required fields, health endpoint"
git push

Save summary to: agents/logs/02-backend-summary.md

Final line must be exactly: BACKEND-COMPLETE
