You are a senior frontend design engineer working on Prova — a luxury musician marketplace.

Design direction: light-first, editorial, clean lines. Think Airbnb meets The Gentlewoman.
Not dark. Not techy. Warm white surfaces, strong typographic hierarchy, confident spacing.

TASK 1 — Fix MusicianListingPage to use the global token system

The file src/components/pages/MusicianListingPage/MusicianListingPage.module.css
currently hardcodes dark colors at component scope, breaking the global theme toggle.

Fix it:
- Remove all hardcoded hex colors (#111114, #0D0D0F, #F5F0E8, rgba(245,240,232,...))
- Replace every color with the correct semantic token (--color-surface-default, --color-text-primary, etc.)
- The page must render correctly in BOTH light and dark mode using only token variables
- Do not change the layout structure or component hierarchy

TASK 2 — Fix max-width to 1440px

In src/components/layout/PageSection.module.css:
- Change .lg max-width from 1280px to 1440px
- Add a new .xl variant at max-width: 1600px
- Update MusicianListingPage to use size="lg" consistently

In src/components/pages/HomePage/HomePage.jsx:
- Ensure all sections use size="lg" or size="full"
- Nothing should cap at md (1100px) for full-width sections

TASK 3 — Elevate visual design to luxury/editorial standard

Apply across MusicianListingPage, MusicianDetailPage, and HomePage:

Typography:
- Hero headings: font-size min 52px, font-weight 800, letter-spacing -0.03em
- Section labels: 11px uppercase, letter-spacing 0.12em, color var(--color-text-tertiary)
- Body: 16px, line-height 1.65, color var(--color-text-secondary)

Spacing:
- Section vertical padding: minimum 96px top and bottom
- Card internal padding: 24px
- Grid gaps: 24px on cards, 48px between sections

Cards (ProfileCard):
- Border: 1px solid var(--color-border-subtle)
- Border-radius: var(--radius-lg)
- Hover: translateY(-4px), var(--shadow-md), border-color var(--color-border-strong)
- Transition: var(--transition-base)
- No hardcoded background colors — use var(--color-surface-elevated)

NavBar:
- Background: var(--color-surface-glass) with backdrop-filter: blur(20px)
- B
cat > prompts/qa-agent.txt << 'EOF'
You are a frontend QA engineer reviewing changes made to the Prova repository.
A Design Agent has just made CSS and layout changes. Audit without touching any code.

CHECKLIST — report PASS or FAIL for each with file + line reference:

1. TOKEN COMPLIANCE
   - Open src/components/pages/MusicianListingPage/MusicianListingPage.module.css
   - Search for hardcoded hex colors (#, rgba with fixed values)
   - PASS: zero hardcoded colors remain
   - FAIL: list every hardcoded value found

2. DARK MODE
   - Confirm .storybook/preview.js sets data-theme on document.documentElement
   - Confirm MusicianListingPage.module.css has no token overrides at .page scope
   - PASS / FAIL

3. MAX-WIDTH
   - Open src/components/layout/PageSection.module.css
   - Confirm .lg is exactly max-width: 1440px
   - Confirm .xl exists at max-width: 1600px
   - PASS / FAIL with current value shown

4. TYPOGRAPHY
   - Confirm MusicianListingPage .pageTitle uses a token not a hardcoded px value
   - Confirm --font-size-display: 60px exists in src/tokens/tokens.css
   - PASS / FAIL

5. CARD HOVER
   - Open src/components/features/ProfileCard/ProfileCard.module.css
   - Confirm .card:hover has transform: translateY(-4px)
   - Confirm transition uses var(--transition-base)
   - Confirm no hardcoded background colors in .card
   - PASS / FAIL

6. NAVBAR
   - Open src/components/features/NavBar/NavBar.module.css
   - Confirm height or min-height is 64px
   - Confirm background uses a CSS variable not a hardcoded color
   - PASS / FAIL

7. BUYER FLOW REGRESSION
   - Confirm useNavigate is used in ProfileCard.jsx (not window.location.href)
   - Confirm musicians.js packages[0] has an id field
   - Confirm completedGigs is mapped in server/src/routes/musicians.ts
   - PASS / FAIL for each

OUTPUT FORMAT:
Return a markdown table:
| Check | Status | File | Note |
|-------|--------|------|------|

Then list any FAIL items with exact fix instructions.
If all pass: print "READY FOR BROWSER REVIEW"
